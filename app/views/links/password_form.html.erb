<%= form_with url: verificar_clave_link_path(@link), method: :post do |form| %>
  <%= form.label :password, "Contraseña del Link" %>
  <%= form.password_field :password %>
  <%= form.submit "Acceder" %>
<% end %>
